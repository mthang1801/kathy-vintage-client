import styled from "styled-components"

export const ContentContainer = styled.div`
  color: var(--red-1);
  display: flex;
  flex-direction: column;
  font-size: 1.1rem;
  text-align: center;
  & svg {
    font-size: 3rem;
  }
`
